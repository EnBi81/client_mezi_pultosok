import {AppRoute} from "../interfaces/AppRoute";

export const AppRoutes: {schedule: AppRoute, settings: AppRoute} = {
    schedule: {
        name: 'Schedule'
    },
    settings: {
        name: 'Settings'
    }
}